# Create a button factory class that renders ios button or android button, based on the operating system.
# The operating system should be given as an argument to the 
# The button should have on click property that prints "I am on iOS" on iOS and "I am on Android" on Android